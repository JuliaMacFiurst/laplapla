# Splash assets

Approved static source: `app-splash-logo-640.webp`.
Authored animation: `laplapla-splash-animated.svg`.

Everything under `generated/` is produced by `npm run generate:splash` and must
not be edited independently. The Android repository owns and validates its
native density resources separately.
